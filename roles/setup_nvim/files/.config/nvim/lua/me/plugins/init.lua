-- Put plugins that require no configuration here, otherwise create
-- a separate `plugin.lua` file in `./lua/me/plugins`.

return {
    -- tmux & split window navigation
    "christoomey/vim-tmux-navigator",
}
